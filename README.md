# cruds-product-management
A complete, vanilla JavaScript web application that implements the CRUDS operations (Create, Read, Update, Delete, Search) for managing product data, using Local Storage for persistence.
